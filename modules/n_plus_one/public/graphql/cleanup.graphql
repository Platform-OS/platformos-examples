mutation {
  company_cleanup: records_delete_all(table: "modules/n_plus_one/company") {
    count
  }

  programmers_cleanup: records_delete_all(table: "modules/n_plus_one/programmer") {
    count
  }
}
