---
slug: contacts
metadata:
  description: Building a contact form with record
  docs_slug: developer-guide/records/building-contact-form-with-record
---
{%- assign id = context.params.slug2 -%}
{%- graphql g = "modules/contacts/get_contacts", id: id -%}

{%- if id == blank -%}
  {% include 'modules/contacts/list', contacts: g.records.results %}
{%- else -%}
  {% include 'modules/contacts/show', contact: g.records.results.first %}
{%- endif -%}
