---
slug: contacts
metadata:
  description: Building a contact form with customization
  docs_slug: tutorials/customizations/building-contact-form-with-customization
---
{%- assign id = context.params.slug2 -%}
{%- graphql g = "modules/contacts/get_contacts", id: id -%}

{%- if id == blank -%}
  {% include 'modules/contacts/list', contacts: g.models.results %}
{%- else -%}
  {% include 'modules/contacts/show', contact: g.models.results.first %}
{%- endif -%}
