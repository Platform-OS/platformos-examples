---
slug: contacts
metadata:
  description: Building a contact form with customization
  docs_slug: tutorials/customizations/building-contact-form-with-customization
---
{%- assign id = context.params.slug2 -%}
{%- query_graph "modules/contacts/get_contacts", result_name: "g", id: id -%}

{%- if id == blank -%}
  {% include 'modules/contacts/list', contacts: g.customizations.results %}
{%- else -%}
  {% include 'modules/contacts/show', contact: g.customizations.results.first %}
{%- endif -%}
