mutation {
  company_cleanup: models_delete_all(model_schema_name: "modules/geolocation/company") {
    count
  }

  programmers_cleanup: models_delete_all(model_schema_name: "modules/geolocation/programmer") {
    count
  }
}
