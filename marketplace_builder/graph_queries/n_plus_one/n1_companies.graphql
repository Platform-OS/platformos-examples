query companies($ids: [ID], $limit: Int = 200) {
  companies: customizations(name: "company", per_page: $limit, ids: $ids){
    total_entries
    results {
      properties
      id
      name
    }
  }
}
