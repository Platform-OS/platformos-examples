
{% form html-data-form: "create" %}
  {% assign p = form_builder.fields.properties %}
  <div class="card">
    <div class="card-header">Create new feedback (customization)</div>
    <div class="card-body">
      <div class="row">
        <label class="col-4">
          <input type="radio" name='{{ p.rate.name }}' value="Excellent">
          Excellent
        </label>
        <label class="col-4">
          <input type="radio" name='{{ p.rate.name }}' value="Meh">
          Meh
        </label>
        <label class="col-4">
          <input type="radio" name='{{ p.rate.name }}' value="Very bad">
          Very bad
        </label>
      </div>

      <div class="w-100"></div>

      <label for="create_message">Message</label>
      <textarea type='text' id="create_message" name='{{ p.message.name }}' class="col-12 form-control"></textarea>

      <button class="btn btn-primary mt-3">Create</button>
    </div>
  </div>
{% endform %}